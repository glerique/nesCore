# docker/frankenphp/Caddyfile
{
    frankenphp
    order php_server before file_server
}

localhost:80 {
    root * /app/public
    php_server
    encode gzip
    file_server
    
    log {
        output stdout
        format console
    }
}